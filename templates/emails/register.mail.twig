<p>Pour activer votre compte, merci de cliquer sur le lien <strong><a href="{{ url('security_confirm', {'id': user.id, 'token': user.token}) }}">{{ url('security_confirm', {'id': user.id, 'token': user.token}) }}</a></strong></p>